import Login from '../Login';
import PageBanner from '../PageBanner';

export default function LoginPage() {
    return (
        <div>
            <PageBanner />
            <Login />
        </div>
    )
}